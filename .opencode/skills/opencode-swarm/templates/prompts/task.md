Task: <one line>.

Read AGENTS.md first and follow it. Do not list or read anything outside the working directory.
Read <CONTRACTS.md section>, <files to read>, and the acceptance tests <tests/...>. The tests are the spec: do not
edit any test.

Edit only:
- <file>: <what to change>
- <file>: <what to change, or "ONLY the X values; nothing else">

Constraints: <performance, style, "read every constant from config; no magic numbers", "update the module
docstring">.

Done when all of these pass:
    <TEST_CMD>
    <LINT_CMD>
<For UI: "and `node --check <file>` passes; mock mode must keep working".>

SPEC:
<paste the relevant spec section>
